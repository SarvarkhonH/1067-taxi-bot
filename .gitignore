node_modules/
dist/
build/
*.log
# Har qanday .env varianti — nusxa/zaxira fayllar ham (masalan .env.neon-backup-…): faqat `.env`
# yozilgan bo'lsa, parallel sessiyaning `git add -A` si zaxira nusxani commit qilib sirlarni
# oshkor qilardi.
.env
.env*
packages/server/prisma/dev.db
packages/server/prisma/dev.db-journal
packages/server/prisma/*.db
.DS_Store
.idea/
.vscode/
kas-discovery/

.claude/settings.local.json

.vercel/

backups/

.qa-bugs.json

.qa-fixes.json

# bir-martalik push-yordamchi (commit qilinmaydi)
_pushHelper.mjs
PUSH_QILISH.bat
zaxira-*/
packages/server/src/scripts/renewSystemRental.ts

# 2026-08-16 audit: prod-irrelevant / sensitive artifacts. A `git add -A` on main auto-deploys,
# so these must never be sweepable into a commit (financial reports, decompiled client, local sim).
BIRJOY_*.pdf
*_HISOBOT_*.pdf
*_QOLLANMA.pdf
client-apk-decomp/
packages/server/src/sim/
sim-out/
birjoy_sim.py
twa.js
PROMPT_SHABLON.md
